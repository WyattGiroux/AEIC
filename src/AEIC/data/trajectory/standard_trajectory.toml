# Trajectories are composed of individual segments which are each sorted into
# classification as "climb," "cruise," or "descent." Below is the standard
# trajectory definition

# IMPORTANT: Descent is defined backwards chronologically. This to assist in finding
# the intercept between cruise and descent.

[climb]
    [climb.CFR_ROCD_Const_Speed]
        cas = 128.6 # 250 knots in m/s
        roc_pos = true

        # `end_alt` accepts either a string or float. The former searches the Builder for the prescribed
        # variable using hasattr and getattr. The latter directly applies the value.
        end_alt = 'crz_start_altitude'

        [climb.CFR_ROCD_Const_Speed.interrupts]
            test = true
            # TBD: will include Mach = crz mach 'next' interrupt here

    [climb.CFR_ROCD_Const_Mach]
        mach = 'cruise_mach'
        roc_pos = true
        end_alt = 'crz_start_altitude'

[cruise]
    [cruise.Const_Alt_Const_Mach]
        alt = 'crz_start_altitude'
        mach = 'cruise_mach'

        # The intersection with descent profile is checked on the builder level and is not included here.

[descent]
    # Descent, again, is defined backwards (going from end altitude to operating ceiling)
    [descent.CFR_ROCD_Const_Speed]
        cas = 128.6 # 250 knots in m/s
        roc_pos = false
        end_alt = 'maximum_altitude'

        [climb.CFR_ROCD_Const_Speed.interrupts]
            test = true
            # TBD: will include Mach = crz mach 'next' interrupt here

    [descent.CFR_ROCD_Const_Mach]
        mach = 'cruise_mach'
        roc_pos = false
        end_alt = 'maximum_altitude'
