# Standard Trajectory Builder

The "standard" trajectory is the interface most users of AEIC not seeking to replicate MATLAB results will use. It works off a modular definition of a vertical trajectory to allow for more realistic trajectory definition. Below is a basic process chart outlining how the standard trajectory builder runs a single flight iteration (the main)

```{image} ../../_static/FlightProcess.webp
:align: center
```
